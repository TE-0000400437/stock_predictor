version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd $CODEBUILD_SRC_DIR/frontend && npm ci
    build:
      commands:
        - cd $CODEBUILD_SRC_DIR/frontend && npx nuxt generate
  artifacts:
    baseDirectory: frontend/.output/public
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
