version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend && npm ci
    build:
      commands:
        - npx nuxt generate
  artifacts:
    baseDirectory: frontend/.amplify-hosting/static
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
