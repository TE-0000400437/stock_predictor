version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend && npm ci
    build:
      commands:
        - cd frontend && npx nuxt generate
  artifacts:
    baseDirectory: frontend/.output/public
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
